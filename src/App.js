import React, { Fragment } from 'react';
import { BrowserRouter as Router, Route } from 'react-router-dom';

const App = () => {
  return (
    <Fragment>
      <Router>
        Hello world!!!
      </Router>
    </Fragment>
  );
};

export default App;